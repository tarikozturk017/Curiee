import React from "react";

const Card = ({ children }) => {
  return (
    <div className="mb-10 text-center text-indigo-200"
    >
        {children}
    </div>
  );
};

export default Card;
